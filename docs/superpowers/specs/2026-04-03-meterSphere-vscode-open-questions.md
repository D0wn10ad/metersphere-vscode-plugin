# Open Questions — Phase 1 & Phase 2

- Confirmation on per-workspace persistence toggle in Phase 2: global default with optional per-workspace toggle? (We used global by default.)
- Finalize API endpoint host in MeterSphere v2 mapping (use real host in production).
- Confirm whether to implement SecretStorage migration plan in Phase 2 or Phase 3.
- Any additional Phase 2 UX preferences (e.g., navigator placement, short-cut bindings) before finalizing the design doc.
